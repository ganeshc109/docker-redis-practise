spring:
  application:
    name: A-user-service
  cloud:
    config:
      uri: http://a-config-server:8888
      fail-fast: true
      retry:
        initial-interval: 1000
        max-attempts: 6
        max-interval: 2000
        multiplier: 1.1
